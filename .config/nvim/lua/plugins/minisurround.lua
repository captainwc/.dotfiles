return {
    -- minisurround 的快捷键时 gs[ardhfF]，而非只有一个s，开始不知道，为了跟跳转的区分才加了这个
    -- 但这会影响s跳转的体验，故删除。留在此处只是为了作为一个配置的例子。获取不久后将会删除
    -- "mini.surround",
    -- mappings = {
    --     add = "sa", -- 添加环绕
    --     delete = "sd", -- 删除环绕
    --     highlight = "sh", -- 高亮环绕
    --     replace = "sr", -- 替换环绕
    -- },
    -- n_lines = 20, -- 搜索环绕的行数
    -- search_method = "cover", -- 搜索方法
}
